apiVersion: v1
kind: Service
metadata:
  name: {{ include "lead-scoring-backend.fullname" . }}
  labels:
    {{- include "lead-scoring-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lead-scoring-backend.selectorLabels" . | nindent 4 }}
